**### Week 1: Infrastructure Initialization and Network Baseline

The primary objective of the first week is to establish the base operating system infrastructure, configure the intermediate communication layers, and achieve a stable publisher/subscriber baseline across all heterogeneous nodes before introducing complex cryptography.

| Execution Phase                           | Operational Directives                                                                                                                                                                                                              | Validation Metric                                                                                                                     |
| ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| Phase 1.1: Supervisor OS Provisioning     | Flash Ubuntu 22.04 LTS natively onto the Raspberry Pi 4, avoiding virtualization to ensure deterministic network timing.1Install ROS 2 Humble Hawksbill.1Configure static IP routing for the localized IEEE 802.11 ad-hoc network.1 | The ros2 topic list command executes locally on the supervisor with zero configuration or daemon errors.                              |
| Phase 1.2: Micro-ROS Build Environment    | Initialize the Arduino IDE or PlatformIO environment for the Cortex-M4 edge nodes. Install the precompiled micro_ros_arduino library.8Ensure the board manager utilizes the correct Mbed OS core for the nRF52840 SoC.4             | Successful compilation and flashing of a blank micro-ROS skeleton sketch targeting the Nano 33 BLE.                                   |
| Phase 1.3: Agent-Client Broker Deployment | Deploy the Micro XRCE-DDS Agent natively on the Raspberry Pi 4. *(Note: Docker containerization moved to Future Work).* Configure the transport layer for UDP over WiFi (or Serial over USB for baseline out-of-band telemetry). | The micro-ROS agent successfully initializes on the specified port without transport binding or socket errors. |
| Phase 1.4: Edge Node Enrollment           | Flash a subset of the Nano 33 BLEs (three nodes) with a basic ROS 2 publisher script (e.g., publishing a std_msgs/Int32 heartbeat). Bind them to the Raspberry Pi 4 agent over the local network.14                                 | ros2 node list on the supervisor displays the three edge nodes; ros2 topic echo shows continuous, uncorrupted data streams.           |
| Phase 1.5: Synthetic Network Degradation  | Deploy the Linux Traffic Control (tc), iptables port-blocking, and Scapy ARP cache poisoning scripts on the wired backend.1Induce an artificial 15% packet-loss rate (![]()).1                                                        | The libpcap/tshark sniffer successfully captures the deterministic baseline message-arrival rate (![]()) and logs the dropped packets.1 |

**
